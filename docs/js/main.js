// ===== i18n =====
const I18N = {
  'zh-CN': {
    skip: '跳到主要内容',
    navHow: '使用流程', navFeatures: '功能特色', navScreenshots: '应用截图', navFaq: '常见问题', navGet: '获取 App',
    heroEyebrow: '✦ AI 助手 · 智能日程管理',
    heroTitle: '让每一天都充满意义',
    heroSubtitle: '记录事件、生日与截止日，AI 助手帮你规划每一天，倒计时和提醒让你不错过任何重要时刻。',
    downloadBtn: 'App Store 下载', learnMore: '了解更多',
    trust1: '🤖 AI 智能助手', trust2: '🔔 灵活提醒', trust3: '🔒 隐私安全',
    heroDeviceSub: '即将到来 · 4',
    dTitle1: '妈妈生日', dTag1: '生日', dDate1: '6月20日',
    dTitle2: '项目截止', dTag2: '工作', dDate2: '6月22日',
    dTitle3: '每月账单日', dTag3: '账单', dDate3: '6月29日',
    dTitle4: '结婚纪念日', dTag4: '纪念', dDate4: '7月15日',
    dUnit: '天后',
    cdFoot: '🔔 3 个提醒已设置',
    chipCdLabel: '距元旦', chipB: '🔔 提醒已开',
    proofRating: '效率工具 · 日程管理',
    stat1: '原生体验', stat2: '本地存储', stat3: '双语界面', stat4: '广告 · 追踪',
    howTitle: '三步不再错过重要时刻', howSub: '添加、规划、提醒，其余交给 Dailymatters。',
    step1Title: '添加事件与截止日', step1Desc: '记录生日、账单、截止日或纪念日，设定分类、标签与重复规则。',
    step2Title: 'AI 助手规划你的一天', step2Desc: '基于你的真实日程，AI 助手给出个性化的时间安排与建议。',
    step3Title: '提醒与小组件不错过', step3Desc: '灵活提醒准时提醒你，桌面小组件随时查看倒计时与今日安排。',
    featuresTitle: '强大功能，简单易用', featuresSub: '为专注和掌控感而设计的每一个细节。',
    feature1Title: 'AI 智能助手', feature1Desc: '基于你的实际日程数据，提供个性化的时间管理建议和智能分析。',
    feature2Title: '倒计时与重复', feature2Desc: '支持多种重复模式，倒计时和已过天数一目了然，重要日子从不遗漏。',
    feature3Title: '智能提醒系统', feature3Desc: '多种提醒方式，自定义提醒时间，确保不错过任何重要时刻。',
    feature4Title: '精美小组件', feature4Desc: '在主屏幕即可查看今日安排和重要倒计时，随时掌握日程动态。',
    feature5Title: '分类与标签', feature5Desc: '丰富的分类系统和自定义标签，让事件管理井井有条。',
    feature6Title: '隐私优先', feature6Desc: '所有数据存储在本地设备，无需担心隐私泄露，安全可靠。',
    screenshotsTitle: '界面预览', screenshotsSub: '左右滑动，点击可放大查看。',
    shot1: '倒计时一目了然', shot2: '轻松创建事件与提醒', shot3: '日历视图与本月概览', shot4: '分类、主题与数据管理',
    privacyTitle: '你的日程，只属于你', privacyDesc: '数据全部保存在设备本地，采用 SwiftData 管理，卸载即彻底清除，无需账号。',
    pp1: '数据本地存储', pp2: '无需注册账号', pp3: '无任何追踪', pp4: '无广告干扰',
    faqTitle: '常见问题',
    q1: 'Dailymatters 是免费的吗？', a1: '是的，Dailymatters 可在 App Store 免费下载使用。',
    q2: '可以设置重复事件吗？', a2: '可以。支持每日、每周、每月、每年等多种重复模式，非常适合生日、账单和日常打卡。',
    q3: '支持哪些提醒方式？', a3: '每个事件都可添加多个自定义提醒，支持当天提醒和提前提醒，确保重要时刻不错过。',
    q4: '我的数据安全吗？', a4: '所有事件数据都保存在设备本地，无需账号。使用 AI 助手时需要你自行配置 API 密钥，对话通过加密连接发送，我们不会存储你的密钥或对话记录。',
    q5: '有桌面小组件吗？', a5: '有。你可以把 Dailymatters 小组件添加到主屏幕，随时查看今日安排与重要倒计时。',
    q6: '支持哪些语言和系统版本？', a6: '支持简体中文与英文双语，需要 iOS 17.0 或更高版本。',
    downloadTitle: '立即下载 Dailymatters', downloadSubtitle: '让 Dailymatters 成为你的智能时间管家。', downloadNote: '支持 iOS 17.0 及以上版本',
    footerPrivacy: '隐私政策', footerTerms: '使用条款', footerSupport: '技术支持', footerContact: '联系我们',
    footerRights: 'Dailymatters. 保留所有权利。', footerMadeBy: 'Built by WeiProduct',
    stickySub: '倒计时 · 提醒 · 免费', stickyGet: '获取'
  },
  'en': {
    skip: 'Skip to content',
    navHow: 'How it works', navFeatures: 'Features', navScreenshots: 'Screenshots', navFaq: 'FAQ', navGet: 'Get App',
    heroEyebrow: '✦ AI assistant · Smart scheduling',
    heroTitle: 'Make Every Day Count',
    heroSubtitle: 'Track events, birthdays and deadlines. Your AI assistant plans your day, while countdowns and reminders keep you from missing what matters.',
    downloadBtn: 'Download on App Store', learnMore: 'Learn more',
    trust1: '🤖 AI assistant', trust2: '🔔 Flexible reminders', trust3: '🔒 Private & secure',
    heroDeviceSub: 'Upcoming · 4',
    dTitle1: "Mom's Birthday", dTag1: 'Birthday', dDate1: 'Jun 20',
    dTitle2: 'Project Deadline', dTag2: 'Work', dDate2: 'Jun 22',
    dTitle3: 'Monthly Bill', dTag3: 'Bill', dDate3: 'Jun 29',
    dTitle4: 'Anniversary', dTag4: 'Memorial', dDate4: 'Jul 15',
    dUnit: 'days',
    cdFoot: '🔔 3 reminders set',
    chipCdLabel: 'New Year in', chipB: '🔔 Reminders on',
    proofRating: 'Productivity · Scheduling',
    stat1: 'Native feel', stat2: 'On-device', stat3: 'Bilingual', stat4: 'Ads · Tracking',
    howTitle: 'Never miss a moment in three steps', howSub: 'Add, plan, get reminded — Dailymatters handles the rest.',
    step1Title: 'Add events & deadlines', step1Desc: 'Log birthdays, bills, deadlines or anniversaries with categories, tags and recurrence.',
    step2Title: 'AI assistant plans your day', step2Desc: 'Based on your real schedule, the AI assistant offers personalized planning and suggestions.',
    step3Title: 'Reminders & widgets keep you on track', step3Desc: 'Flexible reminders alert you on time, and home-screen widgets show countdowns at a glance.',
    featuresTitle: 'Powerful yet simple', featuresSub: 'Every detail designed for focus and control.',
    feature1Title: 'AI Assistant', feature1Desc: 'Personalized time-management suggestions and smart analysis based on your real schedule data.',
    feature2Title: 'Countdown & Recurrence', feature2Desc: 'Multiple recurrence modes with clear countdowns and days-elapsed, so key dates are never missed.',
    feature3Title: 'Smart Reminders', feature3Desc: 'Multiple reminder styles with custom timing, ensuring you never miss an important moment.',
    feature4Title: 'Home Screen Widgets', feature4Desc: "View today's plan and key countdowns right from your home screen, always in the loop.",
    feature5Title: 'Categories & Tags', feature5Desc: 'A rich category system plus custom tags keeps every event neatly organized.',
    feature6Title: 'Privacy First', feature6Desc: 'All data is stored locally on your device — safe, reliable, and private by design.',
    screenshotsTitle: 'Take a look inside', screenshotsSub: 'Swipe through — tap any shot to zoom in.',
    shot1: 'Countdowns at a glance', shot2: 'Create events & reminders easily', shot3: 'Calendar view & monthly overview', shot4: 'Categories, themes & data',
    privacyTitle: 'Your schedule is yours alone', privacyDesc: 'All data stays on your device via SwiftData, wiped completely on uninstall — no account required.',
    pp1: 'Stored on-device', pp2: 'No account needed', pp3: 'Zero tracking', pp4: 'No ads',
    faqTitle: 'Frequently Asked Questions',
    q1: 'Is Dailymatters free?', a1: 'Yes — Dailymatters is free to download and use on the App Store.',
    q2: 'Can I set recurring events?', a2: 'Yes. Daily, weekly, monthly and yearly recurrence are all supported — perfect for birthdays, bills and daily habits.',
    q3: 'What reminder options are there?', a3: 'Each event can have multiple custom reminders, including day-of and advance notices, so nothing slips by.',
    q4: 'Is my data safe?', a4: "All event data is stored locally on your device — no account needed. Using the AI assistant requires your own API key; conversations are sent over an encrypted connection, and we don't store your key or chats.",
    q5: 'Are there home-screen widgets?', a5: 'Yes. Add Dailymatters widgets to your home screen to see today’s plan and key countdowns anytime.',
    q6: 'Which languages and iOS versions are supported?', a6: 'Simplified Chinese and English, requiring iOS 17.0 or later.',
    downloadTitle: 'Download Dailymatters Now', downloadSubtitle: 'Let Dailymatters be your smart time keeper.', downloadNote: 'Requires iOS 17.0 or later',
    footerPrivacy: 'Privacy Policy', footerTerms: 'Terms of Use', footerSupport: 'Support', footerContact: 'Contact',
    footerRights: 'Dailymatters. All rights reserved.', footerMadeBy: 'Built by WeiProduct',
    stickySub: 'Countdowns · Reminders · Free', stickyGet: 'Get'
  }
};

let currentLang = 'zh-CN';

function applyLang(lang) {
  currentLang = I18N[lang] ? lang : 'zh-CN';
  const t = I18N[currentLang];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const k = el.getAttribute('data-i18n');
    if (t[k] !== undefined) el.textContent = t[k];
  });
  document.documentElement.lang = currentLang;
  const ls = document.getElementById('langSwitch');
  if (ls) ls.textContent = currentLang === 'zh-CN' ? 'EN' : '中文';
  try { localStorage.setItem('lang', currentLang); } catch (e) {}
}

function initLang() {
  let saved;
  try { saved = localStorage.getItem('lang'); } catch (e) {}
  if (!saved) saved = (navigator.language || '').toLowerCase().startsWith('zh') ? 'zh-CN' : 'en';
  applyLang(saved);
}

// ===== Theme =====
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  const meta = document.getElementById('themeColorMeta');
  if (meta) meta.setAttribute('content', theme === 'dark' ? '#0B0B0F' : '#007AFF');
  try { localStorage.setItem('theme', theme); } catch (e) {}
}
function initTheme() {
  let saved;
  try { saved = localStorage.getItem('theme'); } catch (e) {}
  if (!saved) saved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  setTheme(saved);
}

// ===== Live countdown chip (to next New Year) =====
function initCountdown() {
  const el = document.getElementById('liveCountdown');
  if (!el) return;
  const tick = () => {
    const now = new Date();
    const target = new Date(now.getFullYear() + 1, 0, 1, 0, 0, 0);
    let diff = Math.max(0, Math.floor((target - now) / 1000));
    const d = Math.floor(diff / 86400); diff -= d * 86400;
    const h = Math.floor(diff / 3600); diff -= h * 3600;
    const m = Math.floor(diff / 60); const s = diff - m * 60;
    const pad = n => String(n).padStart(2, '0');
    const unit = currentLang === 'zh-CN' ? '天' : 'd';
    el.textContent = `${d}${unit} ${pad(h)}:${pad(m)}:${pad(s)}`;
  };
  tick();
  setInterval(tick, 1000);
}

// ===== Gallery =====
function initGallery() {
  const track = document.getElementById('galTrack');
  if (!track) return;
  const shots = Array.from(track.children);
  const dotsWrap = document.getElementById('galDots');
  const prev = document.getElementById('galPrev');
  const next = document.getElementById('galNext');

  shots.forEach((_, i) => {
    const b = document.createElement('button');
    b.type = 'button';
    b.setAttribute('aria-label', 'screenshot ' + (i + 1));
    if (i === 0) b.classList.add('active');
    b.addEventListener('click', () => shots[i].scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' }));
    dotsWrap.appendChild(b);
  });
  const dots = Array.from(dotsWrap.children);

  function activeIndex() {
    const c = track.scrollLeft + track.clientWidth / 2;
    let best = 0, bd = Infinity;
    shots.forEach((s, i) => {
      const center = s.offsetLeft + s.offsetWidth / 2;
      const d = Math.abs(center - c);
      if (d < bd) { bd = d; best = i; }
    });
    return best;
  }
  track.addEventListener('scroll', () => {
    const i = activeIndex();
    dots.forEach((d, j) => d.classList.toggle('active', j === i));
  }, { passive: true });

  const step = () => (shots[1] ? shots[1].offsetLeft - shots[0].offsetLeft : 300);
  if (prev) prev.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }));
  if (next) next.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }));

  // Lightbox
  const lb = document.createElement('div');
  lb.className = 'lightbox';
  lb.innerHTML = '<button class="lightbox-close" aria-label="close">&times;</button><img alt="">';
  document.body.appendChild(lb);
  const lbImg = lb.querySelector('img');
  const close = () => lb.classList.remove('open');
  lb.addEventListener('click', e => { if (e.target === lb || e.target.classList.contains('lightbox-close')) close(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') close(); });
  track.querySelectorAll('.phone').forEach(p => {
    p.addEventListener('click', () => {
      const img = p.querySelector('img');
      lbImg.src = img.src; lbImg.alt = img.alt;
      lb.classList.add('open');
    });
  });
}

// ===== Scroll reveal =====
function initReveal() {
  const els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) { els.forEach(e => e.classList.add('in')); return; }
  const io = new IntersectionObserver((entries) => {
    entries.forEach(en => {
      if (en.isIntersecting) { en.target.classList.add('in'); io.unobserve(en.target); }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  els.forEach((el, i) => { el.style.transitionDelay = (Math.min(i, 6) * 0.05) + 's'; io.observe(el); });
}

// ===== Nav + sticky =====
function initScroll() {
  const nav = document.getElementById('navbar');
  const sticky = document.getElementById('stickyCta');
  const onScroll = () => {
    const y = window.scrollY;
    if (nav) nav.classList.toggle('scrolled', y > 20);
    if (sticky) sticky.classList.toggle('show', y > 620);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();
}

// ===== Init =====
document.addEventListener('DOMContentLoaded', () => {
  initTheme();
  initLang();
  initCountdown();
  initGallery();
  initReveal();
  initScroll();

  const yr = document.getElementById('currentYear');
  if (yr) yr.textContent = new Date().getFullYear();

  const ls = document.getElementById('langSwitch');
  if (ls) ls.addEventListener('click', () => applyLang(currentLang === 'zh-CN' ? 'en' : 'zh-CN'));
  const tt = document.getElementById('themeToggle');
  if (tt) tt.addEventListener('click', () => setTheme(document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark'));

  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', function (e) {
      const id = this.getAttribute('href');
      if (id.length > 1) {
        const target = document.querySelector(id);
        if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      }
    });
  });
});
